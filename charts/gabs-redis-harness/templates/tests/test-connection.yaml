apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gabs-redis-harness.fullname" . }}-test-connection"
  labels:
    {{- include "gabs-redis-harness.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gabs-redis-harness.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
